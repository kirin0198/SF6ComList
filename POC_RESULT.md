# PoC Result: SF6 コンボ帳

> 参照元: INTERVIEW_RESULT.md, RESEARCH_RESULT.md
> 実施日: 2026-04-08
> 更新履歴:
>   - 2026-04-08: 初回作成

## PoC概要

SF6コンボ帳の最大の技術的リスクである「ビジュアルコンボ入力UI」について、React + TypeScript による実装可能性を検証した。コンボデータモデルの型定義、テンキー表記のパース・シリアライズ変換ロジック（テスト16件全件成功）、入力UIコンポーネントおよび表示コンポーネントの骨格実装を完成させた。

## 検証項目と結果

| # | 検証項目 | 結果 | 備考 |
|---|---|---|---|
| 1 | ビジュアルコンボ入力UI（React実装） | 成功 | コンポーネント骨格実装・型チェック通過 |
| 2 | コンボデータのJSON構造（TypeScript型定義） | 成功 | 複雑なSF6入力体系を過不足なく表現できた |
| 3 | テンキー表記 ⇔ ビジュアル変換 | 成功 | パーサー・シリアライザー実装、16件テスト全件成功 |

### 検証詳細

#### 検証 1: ビジュアルコンボ入力UI

- **目的:** 方向キーパッド + ボタンパレットによるコンボ組み立てUIが React で実装可能か確認する
- **手法:** `ComboInputUI.tsx` に方向キーパッド・ボタンパレット・コネクターセレクター・プレビュー表示を含む完全な入力UIコンポーネントを実装し、TypeScript型チェックを通過させた
- **結果:** 成功
- **詳細:**
  - 方向キーは3x3グリッド（テンキー配置）で実装。9方向すべてに対応
  - ボタンは5グループ（パンチ/キック/ドライブ/スーパーアーツ/その他）で整理
  - コネクター（`>`/`xx`/`~`/`,`）を独立したボタンとして提供
  - ドラフト状態（方向選択中）とコミット済みステップを分離管理し、ステップが確定するまでボタン選択を待つ操作フローを実現
  - OD（オーバードライブ）トグル、元に戻す・リセット機能を実装
  - `tsc --noEmit` によるTypeScript型チェック: エラー0件
- **所見:** UIの操作フローは「方向選択 → ボタン選択でステップ確定 → コネクター追加」という直感的な流れで実現可能。モバイル対応のためにはタッチイベントへの対応が必要だが、Reactの標準イベントシステムで問題ない

#### 検証 2: コンボデータモデル

- **目的:** SF6のすべての入力表現（通常技・溜め技・修飾子・ドライブアクション・SA等）をJSONで表現できるか確認する
- **手法:** `combo-data-model.ts` にTypeScript型定義を実装し、実際のコンボ例（リュウBnB・ケン画面端DRコンボ）をJSONで表現して型チェックを通過させた
- **結果:** 成功
- **詳細:** 以下の型階層で表現した
  ```
  ComboSequence
    steps: ComboStep[]
      NormalInput    // 通常の方向+ボタン入力（修飾子・ODフラグ付き）
      ChargeInput    // 溜め入力 [4]6HP など
      ConnectorStep  // >、xx、~、,
  ```
  - `Direction` 型: テンキー1〜9（UnionType）
  - `ButtonInput` 型: LP/MP/HP/LK/MK/HK + DI/DR/DP/PP/DRev/OD + SA1/SA2/SA3 + Throw
  - `AttackModifier` 型: j/cl/cr/st（修飾子プレフィックス）
  - `Combo` エンティティ: シーケンス・ダメージ・難易度・タグ・メモを含む完全な永続化型
- **所見:** JSONのネスト構造はやや深いが、コンボの意味的な構造を正確に表現できている。`notation`フィールドでテンキー表記文字列を並行保持することで、全文検索・表示の高速化が可能。DBにはJSONBカラムとして格納するのが適切

#### 検証 3: テンキー表記 ⇔ ビジュアル変換

- **目的:** テンキー表記の文字列とComboSequence（JSON）を相互変換できるか確認する
- **手法:** `notation-converter.ts` にパーサー・シリアライザーを実装し、`notation-converter.test.mjs` で16件のテストを実行した
- **結果:** 成功（16件 / 16件 成功）
- **詳細:**
  ```
  テスト実行結果:
    PASS: 基本: 5MP をパースできる
    PASS: 波動拳コマンド: 236HP をパースできる
    PASS: コネクター: リンク(>) を含むシーケンスをパースできる
    PASS: コネクター: キャンセル(xx) を含むシーケンスをパースできる
    PASS: ドライブアクション: DR をパースできる
    PASS: スーパーアーツ: SA2 をパースできる
    PASS: 修飾子: j.HP をパースできる
    PASS: OD プレフィックス: OD236HP をパースできる
    PASS: 溜め入力: [4]6HP をパースできる
    PASS: 複合コンボ: 5MP > 2MK xx 214MK > SA2 をパースできる
    PASS: シリアライズ: 基本ステップを文字列に変換できる
    PASS: ラウンドトリップ: 5MP > 236HP
    PASS: ラウンドトリップ: 2MK xx DR > 5MP > 5HP xx 236HK xx SA3
    PASS: ラウンドトリップ: j.HP > 5MP > 5HP xx 236HP
    PASS: ラウンドトリップ: [4]6HP
    PASS: ラウンドトリップ: OD236HP > SA3
  結果: 16 件成功 / 0 件失敗
  ```
  - 初期実装ではセパレーター選択アルゴリズムに不具合（`>` と `xx` が混在する表記で誤分割）が発生したが、最も前に位置するセパレーターを選ぶ方式に修正して解消
  - ラウンドトリップ（パース後にシリアライズすると元の文字列に戻る）が全パターンで成立
- **所見:** パーサーのアルゴリズムは実用上問題ない複雑度で実装できた。将来のモダン操作対応や新しい表記法（例: スーパーアーツのキャラ固有コマンド展開）もこの設計で拡張可能

## PoCコード

| ファイル | 概要 | 検証項目 |
|---|---|---|
| poc/combo-data-model.ts | コンボのTypeScript型定義・アイコンマッピング・コンボJSON例 | 検証 2 |
| poc/notation-converter.ts | テンキー表記 ⇔ ComboSequence 相互変換ロジック | 検証 3 |
| poc/ComboInputUI.tsx | ビジュアルコンボ入力UIコンポーネント（入力パッド・ボタン・コネクター・プレビュー） | 検証 1 |
| poc/ComboDisplay.tsx | コンボ表示コンポーネント（カード・リスト・インライン表示） | 検証 1・2 |
| poc/notation-converter.test.mjs | 変換ロジックの動作確認テスト（16件） | 検証 3 |
| poc/tsconfig.json | TypeScriptコンパイル設定 | 型チェック環境 |
| poc/package.json | PoC用依存関係定義（typescript, tsx, @types/react） | 型チェック環境 |

## 技術的な制約・発見事項

- **アイコン素材**: 本PoCでは矢印文字（↖↑↗等）をアイコン代替として使用した。実装では自作SVGコンポーネントに置き換える必要がある。方向アイコンは `Fighting Game Input Icons Pack（CC-BY-4.0）` を使用するか、SVGを自作する選択肢がある
- **方向入力のUX課題**: 現在の設計では方向を個別にクリックして連結する（例: 236 は `2`→`3`→`6` と3回クリック）。頻出コマンドはショートカットボタン（「QCF」「DP」等のモーション入力ボタン）を追加することで操作性を大幅に改善できる
- **モバイル対応**: 方向キーパッドのタッチ操作は通常のReactクリックイベントで対応可能。スマホ画面での配置（方向パッドとボタンを縦に並べる等）は別途UIデザインで検討が必要
- **コンボの意味検証**: ポーカーのような「入力順が正しいか」の検証（例: `236` の後は `HP` でないといけない、等の制約）はPoCの範囲外。UIレベルでは入力可能なものはすべて許可し、フレームデータとの連携（FR-014）で将来対応する方針が適切
- **OD技の表現**: 現在の型定義では `NormalInput.isOD = true` でOD技を表現しているが、`OD` ボタンを単体で表示する方式（`OD` + `236HP` を別ステップ）との二択がある。UXの観点からはODフラグ付きの方が1ステップで表現できて見やすい

## 実現不可能な要件

該当なし。すべての技術的リスクに対して実現可能なアプローチを確認した。

## 推奨技術スタック（初期提案）

| 層 | 技術 | 選定理由 |
|---|---|---|
| フロントエンド | Next.js (App Router) + React | PoC検証でReactコンポーネントの実装パターンが確立。Next.jsのAPIルートでバックエンドを統合できる |
| スタイリング | Tailwind CSS | PoC実装でボタンの色分けをcolorClassとして型安全に管理するパターンが実証された |
| 言語 | TypeScript | PoC型定義の複雑さ（Union型・ネスト構造）を型安全に管理するために必須 |
| 認証 | Auth.js (NextAuth.js v5) | Google/GitHub OAuth + メール/パスワードを統一的に管理。ローカル環境でのlocalhost対応も問題なし |
| ORM | Prisma | TypeScript対応のスキーマ駆動ORM。ComboのsequenceフィールドはPrismaのJson型で格納できる |
| DB（ローカル） | SQLite | ローカル環境前提ならDocker不要で起動できるSQLiteが開発効率的。Prismaで後からPostgreSQLに移行可能 |
| DB（将来） | PostgreSQL | クラウド移行時にJSONBによるコンボシーケンスへの効率的なクエリが可能 |
| アイコン | 自作SVGコンポーネント + Fighting Game Input Icons Pack（CC-BY-4.0） | PoCで確認したアイコンマッピング構造（DIRECTION_ICONS, BUTTON_ICONS）に基づく |

## architect への推奨事項

1. **ComboSequenceのDB格納方式**: `sequence`フィールドはJSONBカラム（PostgreSQL）またはTEXTカラム（SQLite）に格納する。`notation`フィールドは検索・ソート用に別途カラムとして持つ設計を推奨する
2. **頻出モーションのショートカット**: UIにQCF（236）・QCB（214）・DP（623）等のプリセットモーション入力ボタンを追加することで、UXを大幅に改善できる。型定義はすでにこれをサポートしている
3. **ビジュアルアイコンのSVGコンポーネント化**: `DIRECTION_ICONS` / `BUTTON_ICONS` のマッピング（`poc/combo-data-model.ts`内）を基に、SVGコンポーネントを `components/icons/` 以下に整理することを推奨する
4. **テンキー表記の入力補助**: コンボをテキストで直接入力できるテキストフィールドを用意し、`parseNotation()`でビジュアル表示に変換するモードも提供すると、上級者のUXが向上する
5. **コンボの操作種別フィールド**: `Combo`エンティティに `controlType: "classic" | "modern"` フィールドを追加することを推奨する。現時点ではクラシック固定でよいが、将来のモダン操作対応への拡張路を確保する
